# ASTD-Proyect2

# arima_models.py execution

# python -m scripts.arima_models --frequencies weekly --order 1,1,1 --use-validation
# --frequencies  ->  {daily, hourly, monthly, quarterly, weekly, yearly}
# --use-validation  ->  do forecasting and test it in the validation set